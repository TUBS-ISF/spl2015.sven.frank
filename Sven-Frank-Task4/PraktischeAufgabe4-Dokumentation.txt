Im Rahmen der Aufgabe 4 können folgende Features über BlackBox Plugins
in der Main-Methode "an und abgewählt" werden:
!!!!!!!!!!!!!!!
Die Einbindung der "Plugins" findet sich komplett in der Main-Datei 'src.jgpstrackedit.view.JGPSTrackEdit.java'.
Sämtliche Plugin-Klassen befinden sich im Sub-Paket "Buttons"
!!!!!!!!!!!!!!!


Folgende Funktionen wurden umgeändert und können als Plugins eingebunden werden.

- Speicher & Speichern unter neuem Dateinamen: Für beide Funktionalitäten wurden auf Basis
des ButtonPlugin-Interfaces eigene Klassen erstellt, welche die spezifischen Funktionen der
jeweiligen Buttons bereitstellen.

- Speichern von Kartenauschnitten für die Offline-Verfügbarkeit. Sollte die Funktionalität einbedungen
sein, so ist es möglich aktuelle Kartenauschnitte zu speichern und offline zu verwenden.
Orte der Funktionalität:
	- Button in der Funktionsleiste
	- 'Ansicht -> Aktuellen Kartenausschnitt speichern'
	- 'Kacheldownload'

- Speichern vom aktuellen Bildausschnitt als PNG kann nun auch als Plugin eingebunden werden

Folgende Funktionen wurden ebenfalls als "Plugins" umgesetzt:

- Track teilbar in zwei einzelne Tracks 
- Tracks zusammenführen 
- Track umkehren 
- Track komprimieren

Hierbei handelt es sich zwar um einzelne Features, welche jedoch über ein übergeordneted Plugin
"TrackModification" zusammenverwaltet werden. Denn diese Features stehen in nahem Zusammenhang bzgl.
ihrer Funktion. Somit muss lediglich eine Instanz von "TrackModificationButtons" erzeugt werden und
anschließend dessen Funktion "initButtons" aufgerufen werden. Dadurch werden die "Sub-Plugins" dann
instanziiert.

Test: Ein Test der verschiedenen Konfigurationen, wurde an dieser Stelle ausgelassen, da die Features 
von einander unabhängig sind. Jedes der Featrues kann eigenständig an- oder abgeschaltet werden über die
entsprechende Einbindung der Plugins. Existieren keine Instanzen der Plugins, so werden die Funktionen auch
nicht in der Anwendung bereitgestellt. An dieser Stellen ist anzumerken, dass dieses Mal der komplette Button 
nicht angezeigt wird.


ANMERKUNG: Sämtliche Bereiche in den die Anwendung angepasst wurde, sind durch Kommentarblöcke der 
folgenden Art gekennzeichnet:
//---------------------------
// Beschreibung
Quellcode
//---------------------------